# Copy this file to .env and fill in the values.
# .env is gitignored and must never be committed.

# PostgreSQL password for the ruleforge user
POSTGRES_PASSWORD=change_me

# Seq admin password for first run
SEQ_FIRSTRUN_ADMINPASSWORD=change_me_seq_admin

# pgAdmin credentials (development only, used by docker-compose.override.yml)
PGADMIN_DEFAULT_EMAIL=admin@ruleforge.dev
PGADMIN_DEFAULT_PASSWORD=change_me
